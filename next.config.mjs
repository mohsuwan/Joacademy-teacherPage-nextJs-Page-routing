/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // loaderFile: 'https://admin.joacademy.net',
  images: {
    domains: ["admin.joacademy.net"],
  },

};

export default nextConfig;
